import { Router } from 'express';

export const authRouter = Router();

// Supabase Auth handles signup/login directly. This router is kept as a placeholder
// for future extensions (e.g., token validation) but intentionally exposes no routes.
